project structure:
all html is in folder /static
core logic is within folder core, its the buisness logic, and only allowed to use interfaces to communicate to outside
in folder /adapters there should be all communication to outside

